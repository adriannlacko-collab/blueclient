/**
 * Branding.
 *
 * The product name appears in the titlebar, the window title, the About panel
 * and the launch button subtitle. It is defined once here so renaming the
 * client is a single edit rather than a search across the codebase.
 *
 * Renaming also means updating `productName` and `build.productName` in
 * package.json (installer and executable name) and `title` in src/main/main.js.
 */
export const APP_NAME = 'BlueClient';
